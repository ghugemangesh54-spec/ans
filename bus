#include <stdio.h>

int main() {
    int n, i;

    printf("Enter number of nodes: ");
    scanf("%d", &n);

    printf("Bus Topology:\n");

    for(i = 1; i <= n; i++) {
        printf("Node %d connected to Bus\n", i);
    }

    return 0;
}
