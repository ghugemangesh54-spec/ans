#include <stdio.h>
#include <string.h>

int main() {
    char email[200];

    printf("Enter email content:\n");
    gets(email);   // for full line input

    // Check phishing keywords
    if (strstr(email, "urgent") != NULL ||
        strstr(email, "password") != NULL ||
        strstr(email, "click here") != NULL ||
        strstr(email, "bank") != NULL ||
        strstr(email, "verify") != NULL) {

        printf("\n⚠ Warning: This email may be a PHISHING attempt!\n");
    }
    else {
        printf("\nThis email looks SAFE.\n");
    }

    return 0;
}
